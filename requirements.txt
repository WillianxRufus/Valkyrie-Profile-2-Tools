# Runtime dependencies for vp2_translate.py and vp2_cheats.py.
#
# Both applications use only Python 3.11+ standard-library modules, so there
# are currently no third-party packages for pip to install. Tkinter/Tcl-Tk is
# supplied by the Python installation or operating system rather than PyPI.
